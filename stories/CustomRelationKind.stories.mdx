import { Meta, Story } from "@storybook/addon-docs";

import { Gantt } from "../src";

import { CustomRelationKind } from "./CustomRelationKind";

<Meta title="CustomRelationKind" component={Gantt} />

export const Template = props => {
  return <CustomRelationKind {...props} />;
};

# CustomRelationKind example

<Story name="CustomRelationKind">{Template.bind({})}</Story>
